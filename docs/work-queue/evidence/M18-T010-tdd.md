# M18-T010 TDD execution trace

## Status

This is a post-hoc preservation of the root execution transcript for the
active local task. No durable test-run record was committed before the first
M18 implementation commit, so this file does not prove an earlier commit
chronology and must not be treated as independent acceptance evidence by
itself.

It records only commands and outcomes actually observed during this task's
execution. It contains no external request, payment, account, wallet, signer,
transaction, funding, ATS, allocation, persistence, clearing, HCS, payout,
deployment, or live claim.

## Observed RED/GREEN loops

### Initial public-contract RED

Before `packages/core/src/offering-purchase-lifecycle.ts` and its barrel
exports existed, the root ran:

```text
node --test packages/core/test/offering-purchase-lifecycle.test.mjs
```

The module failed to load because `@tool402/core` did not provide the named
`createOfferingPurchase` export. The focused command therefore exited with one
failed test file. The minimal state machine and its public exports were then
added, after which the focused command passed.

### Reentrant-source RED

After the initial implementation, the focused suite gained an event accessor
that attempted to transition the same issued state during the outer transition.
Before the transient source lock existed, the assertion failed because the
inner call produced a distinct `awaiting_payment` successor. The transient
lock and rejected-read cleanup were then added; the focused suite passed.

### Quote-issuance RED

The focused suite next gained copied, frozen, mutable, proxied, and
accessor-backed quote lookalikes. Before the narrow internal issuance verifier
existed, the copied quote case failed with `Missing expected exception.` The
verifier was then added before all quote-field reads; the focused suite passed.

## Current executable confirmation

The current exact command is:

```text
node --test packages/core/test/offering-purchase-lifecycle.test.mjs
```

At final verification it passed 8/8. Core typecheck/test/lint and the root
quality suite remain separate acceptance evidence.
