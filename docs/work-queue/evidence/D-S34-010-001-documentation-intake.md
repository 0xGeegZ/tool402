# D-S34-010-001 — Public documentation intake

## Decision

Create S34-T010 as a documentation-only POLISH candidate over the exact
UI-S34 paths. The selected PREP-UI-001 Provider RiskScan page is visual
authority only for hierarchy, spacing, card treatment, and responsive
composition; it is not a source, data, route, or capability authority.

## Required truth filter

The public content must describe only the local RiskScan descriptor/endpoint
boundary with its nested declarations object, local ToolLoop and demo routes,
five-step Provider preview, read-only Provider projection, command-admission
distinction, and conditionally gated ATS control. It may not claim a configured
host, payment, settlement, public campaign, funding, issuance, revenue,
payout, return, transaction, deployed asset, or Stage B completion.

## Authority

This decision creates no source authority. S34 can enter test-only RED only
after a fresh independent readiness review and explicit activation. Green
requires a separate independent RED acceptance.
