# S24-T010 test-only activation

The human explicitly directed immediate activation. S24-T010 is `20-active`
only for these RED test paths:

- `apps/web/tests/workspace-shell.test.mjs`; and
- `apps/web/tests/dashboard-workspace-reconciliation.test.mjs`.

No dashboard source, child route, shared shell/navigation, asset, API, Agent,
Provider/Demo, configuration, package, deployment, or video path is authorized.
Independent RED review is required before any GREEN scope is selected.
