# M44-T020 GREEN review

## Result

**CLEAR** — independent review found no Critical, Important, or Minor finding
at source commit `d091be93c07807df990975b553352b04df6b4b56`.

## Reviewed boundary

- `@hashgraph/asset-tokenization-contracts@8.0.0` is pinned with its recorded
  integrity and the Factory JSON artifact is the sole ABI source for
  `deployBond` encoding and `BondDeployed` decoding.
- Existing `viem@2.56.1` encodes the artifact-defined tuple. The local helper
  rejects fixed configuration drift before encoding, maps every selected M42
  field, records the upstream zero-address compatibility treatment, and
  normalizes only valid HI-007 candidate transaction identifiers.
- The real `/provider/deploy` Stage 3 client control imports only the artifact
  and viem, is mounted once, and remains disabled. It contains no wallet,
  provider, RPC, simulation, transaction, or candidate behavior.
- The SDK root import, dotenv/Winston mocks, aliases, BBS externalization, and
  `serverExternalPackages` were removed. No handwritten ABI, polyfill, or
  Hardhat path remains.

## Verification

- Node 22.21.1 focused M44 contract and browser-gate tests: 12/12 passed.
- Node 22.21.1 complete Web suite: 264/264 passed.
- Web typecheck, root lint, clean-install dry-run, queue validation, and
  whitespace check passed.
- A fresh `apps/web/.next` cache was generated by `next dev` under Turbopack.
  Its client chunks contain the contracts artifact/control and contain no SDK,
  dotenv mock, Winston mock, or BBS alias. An isolated browser session reached
  `/provider/deploy` Stage 3 and rendered the disabled “Create revenue note —
  unavailable” control without connecting MetaMask or executing an action.
- `next build` under Turbopack remains host-blocked before the application
  graph by the known CSS-helper worker `binding to a port: Operation not
  permitted` failure. It is recorded as an environment limitation, not a
  production-build or deployment claim.

## Ruling

Accept M44-T020 as the bounded local direct-contracts seam. The selected
architecture closes the browser-SDK bundle approach; `HA-ATS-STAGE-B-001`
remains the sole authority for any provider interaction, signing, transaction,
candidate attachment, or testnet action.
