# S19-T010 — ToolLoop demo prefill

## State

- Tier: POLISH
- Queue state: 60-done
- Dependencies: M01-T040 accepted; M08-T010 accepted; S11-T010 accepted
- Owner: The root owns queue state, catalog, ownership, UI ledger, decisions,
  reviews, commits, and pushes. The source/test paths are exactly the UI-S19
  local targets. Accepted layout, package, lockfile, guided-demo, and ToolLoop
  files are narrow root integration reservations only.
- Human actions: none. This local usability amendment creates no wallet,
  payment, provider, configuration, account, transaction, deployment, or live
  behavior.

## Delivered behavior

The Guided Demo ToolLoop step opens the existing route with only
`demo=tool-loop`. The client reads that one closed selector through nuqs and
fills one fixed editable local fixture. No raw form value is read from the URL.
The normal and unsupported-query routes remain blank, and the existing form is
still the only way to send a request.

## Acceptance

- The durable focused RED contract preceded dependency and source changes.
- The closed query literal, blank fallback, frozen detached fixture values,
  editable form defaults, conditional polite notice, exact Guided Demo href,
  and no automatic request behavior are covered.
- Web/root quality, clean-install, queue/reference/whitespace, and guard
  checks pass. Browser verification covers selected, blank, editable, and
  no-pre-submit-request states; the local accessibility audit is clear.
- Independent task and module reviews are clear.

## Boundary

This card improves demonstration ergonomics only. It does not change
RiskScan's request, payment, result, or terminal-state behavior and grants no
external capability.
