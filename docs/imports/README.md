# Import records

Each active module records its minimum local slice with timestamp, target paths, reason, included/excluded requirements, reviewer, and later amendments. The specification and UI ledgers are append-only runtime evidence.

References in these ledgers must resolve to records committed in this repository.
