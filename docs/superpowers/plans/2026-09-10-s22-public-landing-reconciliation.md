# S22-T010 implementation plan

## Goal

Bring the public `/` landing materially closer to the selected local visual
direction while preserving only truthful, currently committed routes and
capabilities.

## Boundaries

No task may add a product flow, request, client data, configuration read,
wallet/provider/account control, payment, settlement, transaction, analytics,
external link, hosted font, fabricated metric/testimonial/evidence, deployment,
or recording behavior. Stage B remains a truth filter even though it is not a
start dependency for this local POLISH slice.

## Tasks

1. Add a focused test-only RED contract for landing hierarchy, the four exact
   CTA targets, local-only claims, and semantic structure.
2. Reconcile only the root page and its landing components to satisfy the
   accepted RED contract, reusing local branded assets and existing route
   targets.
3. Add at most two selected decorative landing assets if the accepted contract
   requires them; do not alter global assets or dependencies.
4. Run focused and complete Web validation, browser checks for desktop/narrow
   viewports and keyboard focus, then independent task and module reviews.

## Implementation order

This plan is not source authority. The root must first obtain an independent
readiness review, move S22 to `10-ready`, activate only focused test RED paths,
and accept RED before an implementer modifies landing source or assets.
